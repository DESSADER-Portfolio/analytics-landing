mixin slide(props = {})
  - const rootClassName = "slide";
  - const className = props.class || null;
  - const style = props.style || null;
  -
    const classes = [
      rootClassName,
      className,
      "swiper-slide"
    ];

  div(
    class = classes 
    style = style
  )
    block